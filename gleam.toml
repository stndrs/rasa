name = "rasa"
version = "2.0.0"
target = "erlang"

description = "Type-safe ETS tables, queues, and counters for Gleam"
licences = ["MIT"]
repository = { type = "github", user = "stndrs", repo = "rasa" }

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
gleam_erlang = ">= 1.3.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
